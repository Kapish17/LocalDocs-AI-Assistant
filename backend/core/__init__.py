"""Core, UI-independent application logic used by the FastAPI service
(api/main.py) and the CLI (app.py).

Nothing in this package imports fastapi, so it can be used headless from
either front end, a script, or tests.
"""
